# Local File: NotificationServices/src/main/resources/application.properties

spring.application.name=NotificationServices
server.port=8085

# --- Connections to other Microservices ---
# 1. Pulls the DB password and hibernate settings from the Config Server
eureka.instance.prefer-ip-address=true
spring.config.import=optional:configserver:http://localhost:8711
# 2. Registers this service with Eureka
#eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# --- Database URL (Required locally since it isn't in Git) ---
spring.datasource.url=jdbc:mysql://localhost:3306/culture_connect_project?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Harshad#0703
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect


# --- Email Server Settings ---
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=cultureconnectnotify@gmail.com
spring.mail.password=puosmvjavbchyitr

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=false

spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.debug=true

#-Dsun.net.inetaddr.ttl=60
#-Dsun.net.inetaddr.negative.ttl=10

# --- Logging Configuration ---
logging.level.com.cultureconnect.notification=DEBUG
spring.output.ansi.enabled=ALWAYS
